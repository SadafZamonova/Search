import './App.css';

import city from './city'

function App() {
  return (
    <table className='table'>
      <thead >
        <tr>
          
        </tr>
      </thead>
      <tbody >
        {city.map((item) => (

        ))}
      </tbody>
    </table>
  );
}

export default App;
